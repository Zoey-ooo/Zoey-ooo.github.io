<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>旅語 TABIGO — 日英旅遊語言學習</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@200;300;400&family=Shippori+Mincho:wght@400;500;600&family=Cormorant:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Raleway:wght@200;300;400;500;600&display=swap" rel="stylesheet">
<style>
/* ═══════════════════════════════════════════
   DESIGN SYSTEM — Dark Lacquer × Gold Foil
   Inspiration: Japanese urushi lacquerware,
   washi paper, sumi-e ink wash painting
═══════════════════════════════════════════ */
:root {
  --lacquer:     #0e0c0a;
  --lacquer-2:   #161310;
  --lacquer-3:   #1e1a16;
  --lacquer-4:   #282219;
  --gold-deep:   #a07830;
  --gold:        #c8962a;
  --gold-bright: #e8b84b;
  --gold-pale:   #f5dfa0;
  --gold-glow:   rgba(200,150,42,0.15);
  --gold-line:   rgba(200,150,42,0.25);
  --vermillion:  #c0392b;
  --vermillion2: #e74c3c;
  --sage:        #7a9e7e;
  --ash:         #8a8070;
  --ash-light:   #b8aa98;
  --paper:       #f0ebe0;
  --white:       #faf7f0;
  --text-bright: #f0e8d8;
  --text-mid:    #c8bca8;
  --text-dim:    #8a8070;
  --radius:      3px;
  --transition:  0.3s cubic-bezier(0.4,0,0.2,1);
}

* { margin:0; padding:0; box-sizing:border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: 'Raleway', 'Noto Serif JP', sans-serif;
  background: var(--lacquer);
  color: var(--text-bright);
  min-height: 100vh;
  overflow-x: hidden;
  cursor: default;
}

/* ── GLOBAL BACKGROUND ── */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background:
    radial-gradient(ellipse 80% 50% at 50% -10%, rgba(200,150,42,0.08) 0%, transparent 60%),
    radial-gradient(ellipse 50% 80% at 100% 50%, rgba(192,57,43,0.04) 0%, transparent 50%),
    radial-gradient(ellipse 60% 60% at 0% 100%, rgba(122,158,126,0.04) 0%, transparent 50%);
  pointer-events: none;
  z-index: 0;
}

/* SVG noise grain overlay */
body::after {
  content: '';
  position: fixed;
  inset: 0;
  opacity: 0.025;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
  background-size: 200px 200px;
  pointer-events: none;
  z-index: 1;
}

/* ══════════════════════════
   HEADER / NAV
══════════════════════════ */
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  height: 68px;
  padding: 0 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(14,12,10,0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid var(--gold-line);
}

.site-header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 48px;
  right: 48px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--gold) 30%, var(--gold) 70%, transparent);
  opacity: 0.3;
}

.logo {
  display: flex;
  align-items: center;
  gap: 14px;
}

.logo-kanji {
  font-family: 'Shippori Mincho', serif;
  font-size: 26px;
  font-weight: 600;
  color: var(--gold-bright);
  letter-spacing: 0.1em;
  text-shadow: 0 0 30px rgba(232,184,75,0.4);
}

.logo-divider {
  width: 1px;
  height: 24px;
  background: var(--gold-line);
}

.logo-roman {
  font-family: 'Cormorant', serif;
  font-weight: 300;
  font-size: 15px;
  letter-spacing: 0.45em;
  color: var(--text-mid);
  text-transform: uppercase;
}

.header-right {
  display: flex;
  align-items: center;
  gap: 24px;
}

.header-tagline {
  font-family: 'Noto Serif JP', serif;
  font-weight: 200;
  font-size: 11px;
  letter-spacing: 0.35em;
  color: var(--gold);
  opacity: 0.7;
}

.lang-badge {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 11px;
  letter-spacing: 0.2em;
  color: var(--text-dim);
  font-family: 'Raleway', sans-serif;
  font-weight: 400;
}

.lang-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--gold);
  box-shadow: 0 0 8px var(--gold);
  animation: breathe 3s ease infinite;
}

@keyframes breathe {
  0%,100% { opacity:0.7; transform:scale(1); }
  50%      { opacity:1;   transform:scale(1.3); }
}

/* ══════════════════════════
   HERO SECTION
══════════════════════════ */
.hero {
  position: relative;
  z-index: 2;
  padding: 80px 48px 64px;
  text-align: center;
  overflow: hidden;
}

/* Ink wash decorative circles */
.hero-ink {
  position: absolute;
  border-radius: 50%;
  pointer-events: none;
}
.hero-ink-1 {
  width: 600px; height: 600px;
  top: -200px; left: 50%;
  transform: translateX(-50%);
  background: radial-gradient(circle, rgba(200,150,42,0.06) 0%, transparent 70%);
}
.hero-ink-2 {
  width: 300px; height: 300px;
  top: 40px; right: -60px;
  background: radial-gradient(circle, rgba(192,57,43,0.05) 0%, transparent 70%);
}

.hero-season {
  display: inline-block;
  font-family: 'Noto Serif JP', serif;
  font-weight: 200;
  font-size: 11px;
  letter-spacing: 0.5em;
  color: var(--gold);
  border: 1px solid var(--gold-line);
  padding: 6px 20px;
  border-radius: 1px;
  margin-bottom: 32px;
  opacity: 0;
  animation: fadeUp 0.8s 0.1s ease forwards;
}

.hero-title {
  font-family: 'Shippori Mincho', serif;
  font-weight: 400;
  font-size: clamp(42px, 6vw, 80px);
  line-height: 1.1;
  letter-spacing: 0.05em;
  margin-bottom: 16px;
  opacity: 0;
  animation: fadeUp 0.8s 0.25s ease forwards;
}

.hero-title-jp {
  display: block;
  background: linear-gradient(135deg, var(--gold-pale) 0%, var(--gold-bright) 50%, var(--gold) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: none;
  filter: drop-shadow(0 0 40px rgba(200,150,42,0.3));
}

.hero-title-en {
  display: block;
  font-family: 'Cormorant', serif;
  font-weight: 300;
  font-style: italic;
  font-size: clamp(22px, 3vw, 38px);
  color: var(--text-mid);
  letter-spacing: 0.15em;
  margin-top: 8px;
}

.hero-sub {
  font-size: 13px;
  letter-spacing: 0.2em;
  color: var(--text-dim);
  margin-bottom: 48px;
  font-weight: 300;
  opacity: 0;
  animation: fadeUp 0.8s 0.4s ease forwards;
}

/* ── SEARCH BAR ── */
.search-container {
  position: relative;
  max-width: 520px;
  margin: 0 auto 16px;
  opacity: 0;
  animation: fadeUp 0.8s 0.55s ease forwards;
}

.search-input {
  width: 100%;
  padding: 16px 52px 16px 24px;
  background: rgba(30,26,22,0.8);
  border: 1px solid var(--gold-line);
  border-radius: var(--radius);
  font-family: 'Raleway', sans-serif;
  font-size: 14px;
  font-weight: 300;
  letter-spacing: 0.08em;
  color: var(--text-bright);
  outline: none;
  transition: var(--transition);
  backdrop-filter: blur(8px);
}

.search-input::placeholder { color: var(--text-dim); }

.search-input:focus {
  border-color: var(--gold);
  box-shadow: 0 0 0 3px rgba(200,150,42,0.1), 0 8px 32px rgba(0,0,0,0.4);
}

.search-icon {
  position: absolute;
  right: 18px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--gold);
  font-size: 18px;
  pointer-events: none;
  opacity: 0.7;
}

/* ── STATS ROW ── */
.stats-row {
  display: flex;
  justify-content: center;
  gap: 32px;
  margin-top: 16px;
  opacity: 0;
  animation: fadeUp 0.8s 0.65s ease forwards;
}

.stat-item {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 11px;
  letter-spacing: 0.2em;
  color: var(--text-dim);
  font-weight: 400;
}

.stat-num {
  font-family: 'Cormorant', serif;
  font-size: 18px;
  color: var(--gold);
  font-weight: 400;
}

/* ══════════════════════════
   CATEGORY TABS
══════════════════════════ */
.tabs-section {
  position: relative;
  z-index: 2;
  padding: 0 32px 48px;
}

.tabs-scroll {
  display: flex;
  gap: 8px;
  overflow-x: auto;
  padding: 4px 0 12px;
  justify-content: center;
  flex-wrap: wrap;
  scrollbar-width: none;
}

.tabs-scroll::-webkit-scrollbar { display: none; }

.tab {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border: 1px solid rgba(200,150,42,0.2);
  border-radius: var(--radius);
  background: transparent;
  font-family: 'Raleway', sans-serif;
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.25em;
  color: var(--text-dim);
  cursor: pointer;
  transition: var(--transition);
  white-space: nowrap;
  position: relative;
  overflow: hidden;
}

.tab::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(200,150,42,0.08), transparent);
  opacity: 0;
  transition: var(--transition);
}

.tab:hover {
  border-color: rgba(200,150,42,0.5);
  color: var(--gold-bright);
}

.tab:hover::before { opacity: 1; }

.tab.active {
  background: linear-gradient(135deg, rgba(200,150,42,0.2), rgba(200,150,42,0.08));
  border-color: var(--gold);
  color: var(--gold-bright);
  box-shadow: 0 0 20px rgba(200,150,42,0.15);
}

.tab-emoji {
  font-size: 14px;
  line-height: 1;
}

/* ══════════════════════════
   PHRASE CARDS GRID
══════════════════════════ */
.cards-section {
  position: relative;
  z-index: 2;
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 32px 100px;
}

.section-label {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 32px;
}

.section-label-text {
  font-family: 'Noto Serif JP', serif;
  font-weight: 200;
  font-size: 10px;
  letter-spacing: 0.5em;
  color: var(--gold);
  white-space: nowrap;
  text-transform: uppercase;
}

.section-label-line {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, var(--gold-line), transparent);
}

.section-count {
  font-family: 'Cormorant', serif;
  font-size: 13px;
  color: var(--text-dim);
  letter-spacing: 0.1em;
}

.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
  gap: 16px;
}

/* ── PHRASE CARD ── */
.card {
  position: relative;
  background: var(--lacquer-2);
  border: 1px solid rgba(200,150,42,0.12);
  border-radius: var(--radius);
  padding: 28px 28px 22px;
  cursor: pointer;
  transition: var(--transition);
  overflow: hidden;
  opacity: 0;
  animation: fadeUp 0.5s ease forwards;
}

/* Left accent bar */
.card::before {
  content: '';
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 2px;
  background: linear-gradient(180deg, var(--gold), transparent);
  transform: scaleY(0);
  transform-origin: top;
  transition: transform 0.4s ease;
}

/* Hover glow */
.card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse at 0% 0%, rgba(200,150,42,0.06) 0%, transparent 60%);
  opacity: 0;
  transition: var(--transition);
}

.card:hover {
  border-color: rgba(200,150,42,0.35);
  transform: translateY(-3px);
  box-shadow: 0 20px 60px rgba(0,0,0,0.5), 0 0 40px rgba(200,150,42,0.08);
}

.card:hover::before { transform: scaleY(1); }
.card:hover::after  { opacity: 1; }

/* Category tag */
.card-cat {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 9px;
  letter-spacing: 0.3em;
  color: var(--gold);
  border: 1px solid rgba(200,150,42,0.25);
  padding: 3px 10px;
  border-radius: 1px;
  margin-bottom: 18px;
  font-weight: 500;
  text-transform: uppercase;
  position: relative;
  z-index: 1;
}

/* Japanese phrase — MAIN */
.card-jp {
  font-family: 'Shippori Mincho', serif;
  font-size: 26px;
  font-weight: 400;
  color: var(--text-bright);
  line-height: 1.5;
  margin-bottom: 6px;
  letter-spacing: 0.05em;
  position: relative;
  z-index: 1;
}

/* Romanization */
.card-roma {
  font-family: 'Cormorant', serif;
  font-style: italic;
  font-size: 13px;
  color: var(--gold);
  margin-bottom: 14px;
  letter-spacing: 0.1em;
  position: relative;
  z-index: 1;
}

/* English translation */
.card-en {
  font-size: 13px;
  font-weight: 300;
  letter-spacing: 0.1em;
  color: var(--text-mid);
  margin-bottom: 5px;
  position: relative;
  z-index: 1;
}

/* Chinese translation */
.card-zh {
  font-size: 12px;
  color: var(--text-dim);
  letter-spacing: 0.06em;
  position: relative;
  z-index: 1;
}

/* Card footer with speak buttons */
.card-footer {
  display: flex;
  gap: 8px;
  margin-top: 20px;
  padding-top: 18px;
  border-top: 1px solid rgba(200,150,42,0.1);
  position: relative;
  z-index: 1;
}

.speak-btn {
  display: flex;
  align-items: center;
  gap: 7px;
  padding: 8px 14px;
  border-radius: var(--radius);
  font-family: 'Raleway', sans-serif;
  font-size: 10px;
  font-weight: 500;
  letter-spacing: 0.25em;
  cursor: pointer;
  transition: var(--transition);
  border: 1px solid;
  text-transform: uppercase;
}

.speak-jp {
  background: rgba(200,150,42,0.1);
  border-color: rgba(200,150,42,0.3);
  color: var(--gold);
}

.speak-jp:hover {
  background: rgba(200,150,42,0.2);
  border-color: var(--gold);
  box-shadow: 0 0 16px rgba(200,150,42,0.2);
}

.speak-en {
  background: rgba(122,158,126,0.08);
  border-color: rgba(122,158,126,0.25);
  color: var(--sage);
}

.speak-en:hover {
  background: rgba(122,158,126,0.15);
  border-color: var(--sage);
}

.speak-btn.playing {
  animation: speakPulse 0.7s ease infinite alternate;
}

.speak-jp.playing  { background: rgba(200,150,42,0.25); border-color: var(--gold-bright); color: var(--gold-bright); }
.speak-en.playing  { background: rgba(122,158,126,0.25); border-color: var(--sage);       color: #a8d4ac; }

@keyframes speakPulse {
  from { box-shadow: 0 0 0 0 currentColor; opacity:0.8; }
  to   { box-shadow: 0 0 12px 3px currentColor; opacity:1; }
}

/* Sound wave icon */
.wave-icon {
  display: inline-flex;
  align-items: flex-end;
  gap: 2px;
  height: 12px;
}
.wave-icon span {
  display: block;
  width: 2.5px;
  border-radius: 2px;
  background: currentColor;
  transition: var(--transition);
}
.wave-icon span:nth-child(1) { height: 5px; }
.wave-icon span:nth-child(2) { height: 9px; }
.wave-icon span:nth-child(3) { height: 7px; }
.wave-icon span:nth-child(4) { height: 11px; }
.wave-icon span:nth-child(5) { height: 5px; }

.speak-btn.playing .wave-icon span {
  animation: waveBar 0.5s ease infinite alternate;
}
.speak-btn.playing .wave-icon span:nth-child(1) { animation-delay: 0s; }
.speak-btn.playing .wave-icon span:nth-child(2) { animation-delay: 0.1s; }
.speak-btn.playing .wave-icon span:nth-child(3) { animation-delay: 0.2s; }
.speak-btn.playing .wave-icon span:nth-child(4) { animation-delay: 0.15s; }
.speak-btn.playing .wave-icon span:nth-child(5) { animation-delay: 0.05s; }

@keyframes waveBar {
  from { transform: scaleY(0.4); }
  to   { transform: scaleY(1.4); }
}

/* ══════════════════════════
   DETAIL MODAL
══════════════════════════ */
.overlay {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 200;
  background: rgba(6,5,4,0.75);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  align-items: center;
  justify-content: center;
  padding: 24px;
}

.overlay.open { display: flex; }

.modal {
  position: relative;
  background: var(--lacquer-3);
  border: 1px solid rgba(200,150,42,0.3);
  border-radius: var(--radius);
  max-width: 560px;
  width: 100%;
  padding: 44px 44px 36px;
  animation: modalAppear 0.35s cubic-bezier(0.34,1.56,0.64,1) forwards;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 40px 120px rgba(0,0,0,0.7), 0 0 60px rgba(200,150,42,0.05);
}

.modal::before {
  content: '';
  position: absolute;
  top: 0; left: 44px; right: 44px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--gold) 30%, var(--gold) 70%, transparent);
  opacity: 0.5;
}

@keyframes modalAppear {
  from { opacity:0; transform: scale(0.93) translateY(20px); }
  to   { opacity:1; transform: scale(1)    translateY(0); }
}

.modal-close {
  position: absolute;
  top: 20px; right: 20px;
  width: 36px; height: 36px;
  background: rgba(200,150,42,0.08);
  border: 1px solid rgba(200,150,42,0.2);
  border-radius: var(--radius);
  color: var(--text-dim);
  font-size: 16px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition);
}

.modal-close:hover {
  background: rgba(192,57,43,0.15);
  border-color: var(--vermillion);
  color: var(--vermillion2);
}

.modal-cat {
  font-size: 9px;
  letter-spacing: 0.4em;
  color: var(--gold);
  text-transform: uppercase;
  margin-bottom: 24px;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 10px;
}

.modal-cat::after {
  content: '';
  flex: 1;
  height: 1px;
  background: var(--gold-line);
}

.modal-jp-big {
  font-family: 'Shippori Mincho', serif;
  font-size: 40px;
  font-weight: 400;
  color: var(--text-bright);
  line-height: 1.4;
  margin-bottom: 8px;
  letter-spacing: 0.05em;
}

.modal-roma {
  font-family: 'Cormorant', serif;
  font-style: italic;
  font-size: 17px;
  color: var(--gold);
  margin-bottom: 28px;
  letter-spacing: 0.12em;
}

.modal-separator {
  display: flex;
  align-items: center;
  gap: 12px;
  margin: 24px 0;
}

.modal-separator-line {
  flex: 1;
  height: 1px;
  background: rgba(200,150,42,0.15);
}

.modal-separator-diamond {
  color: var(--gold);
  font-size: 8px;
  opacity: 0.6;
}

.modal-en-big {
  font-family: 'Cormorant', serif;
  font-size: 26px;
  font-weight: 400;
  color: var(--text-bright);
  line-height: 1.5;
  margin-bottom: 8px;
  letter-spacing: 0.05em;
}

.modal-zh {
  font-size: 15px;
  color: var(--text-mid);
  letter-spacing: 0.08em;
  margin-bottom: 32px;
}

/* Modal speak buttons */
.modal-btns {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-bottom: 28px;
}

.modal-speak {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 14px 20px;
  border-radius: var(--radius);
  font-family: 'Raleway', sans-serif;
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.2em;
  cursor: pointer;
  transition: var(--transition);
  text-transform: uppercase;
}

.modal-speak-jp {
  background: linear-gradient(135deg, rgba(200,150,42,0.2), rgba(200,150,42,0.08));
  border: 1px solid rgba(200,150,42,0.4);
  color: var(--gold-bright);
}

.modal-speak-jp:hover {
  background: linear-gradient(135deg, rgba(200,150,42,0.3), rgba(200,150,42,0.15));
  box-shadow: 0 8px 24px rgba(200,150,42,0.2);
}

.modal-speak-en {
  background: linear-gradient(135deg, rgba(122,158,126,0.15), rgba(122,158,126,0.05));
  border: 1px solid rgba(122,158,126,0.3);
  color: var(--sage);
}

.modal-speak-en:hover {
  background: linear-gradient(135deg, rgba(122,158,126,0.25), rgba(122,158,126,0.1));
}

.modal-speak.playing {
  animation: speakPulse 0.7s ease infinite alternate;
}

/* Tip / usage note */
.modal-tip {
  background: rgba(200,150,42,0.05);
  border: 1px solid rgba(200,150,42,0.15);
  border-left: 3px solid var(--gold);
  border-radius: var(--radius);
  padding: 16px 20px;
  font-size: 12px;
  line-height: 1.8;
  color: var(--text-mid);
  letter-spacing: 0.05em;
}

.modal-tip-label {
  display: block;
  font-size: 9px;
  letter-spacing: 0.4em;
  color: var(--gold);
  text-transform: uppercase;
  margin-bottom: 6px;
  font-weight: 600;
}

/* ══════════════════════════
   EMPTY STATE
══════════════════════════ */
.empty-state {
  grid-column: 1 / -1;
  text-align: center;
  padding: 80px 20px;
}

.empty-state-jp {
  font-family: 'Shippori Mincho', serif;
  font-size: 28px;
  color: var(--text-dim);
  letter-spacing: 0.2em;
  margin-bottom: 8px;
}

.empty-state-en {
  font-size: 13px;
  letter-spacing: 0.2em;
  color: var(--text-dim);
  opacity: 0.5;
  font-weight: 300;
}

/* ══════════════════════════
   FOOTER
══════════════════════════ */
.site-footer {
  position: relative;
  z-index: 2;
  border-top: 1px solid rgba(200,150,42,0.15);
  padding: 32px 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 12px;
}

.footer-brand {
  font-family: 'Shippori Mincho', serif;
  font-size: 20px;
  color: var(--gold);
  opacity: 0.6;
  letter-spacing: 0.15em;
}

.footer-info {
  font-size: 11px;
  letter-spacing: 0.2em;
  color: var(--text-dim);
  font-weight: 300;
}

/* ══════════════════════════
   SCROLLBAR
══════════════════════════ */
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: var(--lacquer-2); }
::-webkit-scrollbar-thumb { background: var(--gold-deep); border-radius: 4px; }

/* ══════════════════════════
   ANIMATIONS
══════════════════════════ */
@keyframes fadeUp {
  from { opacity:0; transform: translateY(24px); }
  to   { opacity:1; transform: translateY(0); }
}

/* ══════════════════════════
   RESPONSIVE
══════════════════════════ */
@media (max-width: 768px) {
  .site-header { padding: 0 20px; }
  .header-tagline { display: none; }
  .hero { padding: 60px 20px 48px; }
  .tabs-section { padding: 0 16px 36px; }
  .tabs-scroll { justify-content: flex-start; flex-wrap: nowrap; }
  .cards-section { padding: 0 16px 80px; }
  .cards-grid { grid-template-columns: 1fr; }
  .modal { padding: 32px 24px 28px; }
  .modal-jp-big { font-size: 30px; }
  .modal-btns { grid-template-columns: 1fr; }
  .site-footer { padding: 24px 20px; }
}
</style>
</head>
<body>

<!-- ══════════════════ HEADER ══════════════════ -->
<header class="site-header">
  <div class="logo">
    <span class="logo-kanji">旅語</span>
    <div class="logo-divider"></div>
    <span class="logo-roman">Tabigo</span>
  </div>
  <div class="header-right">
    <span class="header-tagline">旅行者のための日英語</span>
    <div class="lang-badge">
      <div class="lang-dot"></div>
      JA · EN · ZH
    </div>
  </div>
</header>

<!-- ══════════════════ HERO ══════════════════ -->
<section class="hero">
  <div class="hero-ink hero-ink-1"></div>
  <div class="hero-ink hero-ink-2"></div>

  <div class="hero-season">✦ 旅の言葉 — Phrases for the Wanderer ✦</div>

  <h1 class="hero-title">
    <span class="hero-title-jp">日英旅遊語言</span>
    <span class="hero-title-en">Travel Language Guide</span>
  </h1>

  <p class="hero-sub">精選觀光常用詞彙 · 一鍵發音學習 · 情境分類整理</p>

  <div class="search-container">
    <input
      type="text"
      class="search-input"
      id="searchInput"
      placeholder="搜尋詞彙 / 日本語 / Search phrases..."
      autocomplete="off"
    >
    <span class="search-icon">🔍</span>
  </div>

  <div class="stats-row">
    <div class="stat-item">
      <span class="stat-num" id="totalCount">0</span>
      <span>Phrases</span>
    </div>
    <div class="stat-item">
      <span class="stat-num">9</span>
      <span>Categories</span>
    </div>
    <div class="stat-item">
      <span class="stat-num">3</span>
      <span>Languages</span>
    </div>
  </div>
</section>

<!-- ══════════════════ CATEGORY TABS ══════════════════ -->
<section class="tabs-section">
  <div class="tabs-scroll" id="tabsScroll">
    <button class="tab active" data-cat="all" onclick="setCategory('all',this)">
      <span class="tab-emoji">◈</span> 全部 ALL
    </button>
    <button class="tab" data-cat="greeting" onclick="setCategory('greeting',this)">
      <span class="tab-emoji">🌸</span> 問候
    </button>
    <button class="tab" data-cat="airport" onclick="setCategory('airport',this)">
      <span class="tab-emoji">✈️</span> 機場
    </button>
    <button class="tab" data-cat="hotel" onclick="setCategory('hotel',this)">
      <span class="tab-emoji">🏯</span> 住宿
    </button>
    <button class="tab" data-cat="restaurant" onclick="setCategory('restaurant',this)">
      <span class="tab-emoji">🍜</span> 餐飲
    </button>
    <button class="tab" data-cat="transport" onclick="setCategory('transport',this)">
      <span class="tab-emoji">🚅</span> 交通
    </button>
    <button class="tab" data-cat="shopping" onclick="setCategory('shopping',this)">
      <span class="tab-emoji">🛍️</span> 購物
    </button>
    <button class="tab" data-cat="sightseeing" onclick="setCategory('sightseeing',this)">
      <span class="tab-emoji">⛩️</span> 觀光
    </button>
    <button class="tab" data-cat="emergency" onclick="setCategory('emergency',this)">
      <span class="tab-emoji">🚨</span> 緊急
    </button>
  </div>
</section>

<!-- ══════════════════ CARDS ══════════════════ -->
<section class="cards-section">
  <div class="section-label">
    <span class="section-label-text">フレーズ一覧 · Phrase Library</span>
    <div class="section-label-line"></div>
    <span class="section-count" id="showingCount">— phrases</span>
  </div>
  <div class="cards-grid" id="cardsGrid"></div>
</section>

<!-- ══════════════════ DETAIL MODAL ══════════════════ -->
<div class="overlay" id="overlay" onclick="handleOverlayClick(event)">
  <div class="modal" id="modal">
    <button class="modal-close" onclick="closeModal()">✕</button>

    <div class="modal-cat" id="mCat">Category</div>

    <div class="modal-jp-big" id="mJp">—</div>
    <div class="modal-roma" id="mRoma">—</div>

    <div class="modal-separator">
      <div class="modal-separator-line"></div>
      <span class="modal-separator-diamond">◆</span>
      <div class="modal-separator-line"></div>
    </div>

    <div class="modal-en-big" id="mEn">—</div>
    <div class="modal-zh" id="mZh">—</div>

    <div class="modal-btns">
      <button class="modal-speak modal-speak-jp" id="mBtnJp" onclick="speakModal('jp')">
        <div class="wave-icon">
          <span></span><span></span><span></span><span></span><span></span>
        </div>
        日本語で発音
      </button>
      <button class="modal-speak modal-speak-en" id="mBtnEn" onclick="speakModal('en')">
        <div class="wave-icon">
          <span></span><span></span><span></span><span></span><span></span>
        </div>
        Speak English
      </button>
    </div>

    <div class="modal-tip">
      <span class="modal-tip-label">✦ 使用提示 · Usage Tip</span>
      <span id="mTip"></span>
    </div>
  </div>
</div>

<!-- ══════════════════ FOOTER ══════════════════ -->
<footer class="site-footer">
  <div class="footer-brand">旅語</div>
  <div class="footer-info">Powered by Web Speech API · 點擊卡片展開詳情 · Tap card for details</div>
</footer>

<script>
// ═══════════════════════════════════════════════
//   DATA — Travel Phrase Library
// ═══════════════════════════════════════════════
const PHRASES = [
  // ── GREETINGS ──
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'こんにちは', roma:'Konnichiwa',
    en:'Hello / Good afternoon', zh:'你好 ／ 午安',
    tip:'日語中最通用的問候語，適用於白天任何時段。在商店或餐廳進入時使用也很自然。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'おはようございます', roma:'Ohayou gozaimasu',
    en:'Good morning', zh:'早安',
    tip:'早上十點前使用的正式問候。非正式場合可縮短說「おはよう (Ohayou)」。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'こんばんは', roma:'Konbanwa',
    en:'Good evening', zh:'晚安（問候）',
    tip:'日落後使用的問候語，比「おやすみなさい（晚安・道別）」早使用。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'ありがとうございます', roma:'Arigatou gozaimasu',
    en:'Thank you very much', zh:'非常感謝',
    tip:'正式的感謝語，適用所有場合。非正式可說「ありがとう」。日本人非常重視禮節，多說感謝絕不失禮！'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'すみません', roma:'Sumimasen',
    en:'Excuse me / I\'m sorry', zh:'不好意思 ／ 對不起',
    tip:'萬用語！可用於：呼喚服務員、向人借道、輕微道歉。進入擁擠場所時也常說。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'はじめまして', roma:'Hajimemashite',
    en:'Nice to meet you', zh:'初次見面，請多指教',
    tip:'第一次與人見面時使用，後面通常接「よろしくお願いします (Yoroshiku onegaishimasu)」表示期待往後的關係。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'さようなら', roma:'Sayounara',
    en:'Goodbye', zh:'再見',
    tip:'正式道別用語，帶有「此後一段時間不再相見」的語感。日常告別多說「またね (Mata ne)」更自然。'
  },
  {
    cat:'greeting', catLabel:'問候 · 挨拶',
    jp:'おやすみなさい', roma:'Oyasuminasai',
    en:'Good night', zh:'晚安（道別）',
    tip:'就寢前或夜晚道別時使用，帶有「祝您好眠」的溫暖語感。'
  },

  // ── AIRPORT ──
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'搭乗口はどこですか？', roma:'Tōjō-guchi wa doko desu ka?',
    en:'Where is the boarding gate?', zh:'登機口在哪裡？',
    tip:'指著登機証上的閘門號碼詢問更有效率。日本機場通常有中英日三語標示，但人工詢問更保險。'
  },
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'荷物を預けたいです', roma:'Nimotsu wo azuketai desu',
    en:'I\'d like to check my baggage', zh:'我想要託運行李',
    tip:'「荷物 (nimotsu)」=行李，「預ける (azukeru)」=寄放。手提行李叫「手荷物 (te-nimotsu)」。'
  },
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'免税店はどこですか？', roma:'Menzei-ten wa doko desu ka?',
    en:'Where is the duty-free shop?', zh:'免稅店在哪裡？',
    tip:'記得攜帶護照！日本機場免稅店種類豐富，化妝品、零食、酒類皆可購買。'
  },
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'乗り継ぎはどこですか？', roma:'Noritsugiga wa doko desu ka?',
    en:'Where is the transit / transfer?', zh:'轉機在哪裡辦理？',
    tip:'轉機時常需要重新通過安全檢查，留意指示牌「乗継 (Noritsugi)」，羽田和成田都設有清晰指引。'
  },
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'フライトが遅延しています', roma:'Furaito ga chien shite imasu',
    en:'The flight is delayed', zh:'班機延誤了',
    tip:'若遇班機延誤，可持登機証前往航空公司服務台（カウンター）詢問最新情況及補償措施。'
  },
  {
    cat:'airport', catLabel:'機場 · 空港',
    jp:'この荷物は手荷物として持ち込めますか？', roma:'Kono nimotsu wa te-nimotsu toshite mochi-komemasu ka?',
    en:'Can I bring this as carry-on luggage?', zh:'這個行李可以當手提行李攜帶嗎？',
    tip:'日本航空對液體有嚴格規定（100ml以下且需裝入透明袋），購買液體伴手禮時要特別注意。'
  },

  // ── HOTEL ──
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'チェックインをお願いします', roma:'Chekku-in wo onegaishimasu',
    en:'Check-in please', zh:'請幫我辦理入住',
    tip:'到達飯店首要任務！帶上護照和預訂確認信（電子版也可），日本飯店通常入住時間為下午三時後。'
  },
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'予約した者です', roma:'Yoyaku shita mono desu',
    en:'I have a reservation', zh:'我有預約訂房',
    tip:'後面加上姓名更完整：「〇〇と申します (Mōshimasu)」。日本飯店對預訂記錄管理非常精確。'
  },
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'部屋のカードキーをなくしました', roma:'Heya no kādo-kī wo nakushimashita',
    en:'I lost my room key card', zh:'我弄丟了房卡',
    tip:'立刻通知前台（フロント）。需出示護照或訂房資料核實身份，通常可免費補辦新卡。'
  },
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'WiFiのパスワードを教えてください', roma:'WiFi no pasuwādo wo oshiete kudasai',
    en:'Please tell me the WiFi password', zh:'請告訴我WiFi密碼',
    tip:'大多數日本飯店提供免費WiFi，密碼通常印在歡迎卡或可在前台詢問。'
  },
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'朝食は何時からですか？', roma:'Chōshoku wa nanji kara desu ka?',
    en:'What time does breakfast start?', zh:'早餐幾點開始？',
    tip:'日本旅館（ryokan）的早餐特別精緻，包含和食套餐，通常在七至九時供應，值得特別期待！'
  },
  {
    cat:'hotel', catLabel:'住宿 · ホテル',
    jp:'チェックアウトを延長できますか？', roma:'Chekku-auto wo enchō dekimasu ka?',
    en:'Can I extend check-out time?', zh:'可以延遲退房時間嗎？',
    tip:'日本飯店標準退房時間通常為上午十一時或中午。延遲退房可能需額外費用，建議提前詢問。'
  },

  // ── RESTAURANT ──
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'一人です', roma:'Hitori desu',
    en:'Just one person', zh:'我一個人',
    tip:'進餐廳時工作人員會問「何名様ですか？(Nan-mei-sama desu ka?)」，用「一人/二人/三人」回答。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'これをください', roma:'Kore wo kudasai',
    en:'I\'ll have this one, please', zh:'我要這個',
    tip:'點餐最萬用！直接指著菜單圖片說這句話，不需要讀出日文菜名，非常方便。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'お会計をお願いします', roma:'Okaikei wo onegaishimasu',
    en:'Bill please', zh:'請結帳',
    tip:'日本通常到收銀台付款而非在座位付。可在桌上找到「お会計」(結帳)按鈕或舉手示意。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'アレルギーがあります', roma:'Arerugī ga arimasu',
    en:'I have allergies', zh:'我有食物過敏',
    tip:'後面加過敏原更明確：「えびアレルギーがあります (shrimp allergy)」。也可使用過敏卡（アレルギーカード）。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'おいしいです！', roma:'Oishii desu!',
    en:'It\'s delicious!', zh:'好吃！',
    tip:'日本廚師和服務員聽到外國人讚美食物會非常開心，勇敢說出口！配上比讚手勢效果更好。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'水をいただけますか？', roma:'Mizu wo itadakemasu ka?',
    en:'May I have some water?', zh:'可以給我水嗎？',
    tip:'日本餐廳通常提供免費冷水（お冷 o-hiya）或熱茶（お茶 o-cha），可直接開口索取。'
  },
  {
    cat:'restaurant', catLabel:'餐飲 · 食事',
    jp:'別々に払えますか？', roma:'Betsu-betsu ni haraemasu ka?',
    en:'Can we pay separately?', zh:'可以分開付款嗎？',
    tip:'日本也可以分開付帳，但建議事先說明，有些小店較難分開計算。'
  },

  // ── TRANSPORT ──
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'〜駅はどこですか？', roma:'〜-eki wa doko desu ka?',
    en:'Where is 〜 station?', zh:'〜車站在哪裡？',
    tip:'將「〜」換成車站名，例如「渋谷駅 (Shibuya-eki)」。日本地鐵非常準時，善加利用！'
  },
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'〜まで一枚ください', roma:'〜 made ichimai kudasai',
    en:'One ticket to 〜 please', zh:'到〜一張票，謝謝',
    tip:'在有人工售票口的車站使用。大多數情況建議購買Suica或PASMO感應卡，省去每次買票的麻煩。'
  },
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'このバスは〜に止まりますか？', roma:'Kono basu wa 〜 ni tomarimasu ka?',
    en:'Does this bus stop at 〜?', zh:'這班公車有停〜嗎？',
    tip:'日本公車按站點計費，上車前確認路線很重要。大城市也有Google Maps可即時查詢公車路線。'
  },
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'〜へ行ってください', roma:'〜 e itte kudasai',
    en:'Please take me to 〜', zh:'請到〜',
    tip:'搭計程車時使用，可配合出示手機地圖或地址。日本計程車門是自動開關，請勿自己拉門！'
  },
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'次の電車はいつですか？', roma:'Tsugi no densha wa itsu desu ka?',
    en:'When is the next train?', zh:'下一班電車是什麼時候？',
    tip:'日本電車非常準時，誤差通常在一分鐘以內。站台的電子看板有即時時刻顯示，非常方便。'
  },
  {
    cat:'transport', catLabel:'交通 · 交通機関',
    jp:'乗り換えはどこですか？', roma:'Norikaewa wa doko desu ka?',
    en:'Where do I transfer?', zh:'轉車在哪裡換乘？',
    tip:'日本地鐵站換乘標示清楚，通常有顏色編碼。但東京站等大型換乘站可能需要走較長距離。'
  },

  // ── SHOPPING ──
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'いくらですか？', roma:'Ikura desu ka?',
    en:'How much is it?', zh:'這個多少錢？',
    tip:'最基本的購物用語！日本消費稅為10%，部分標示為「税抜 (稅前)」，結帳時需加算稅金。'
  },
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'試着してもいいですか？', roma:'Shichaku shite mo ii desu ka?',
    en:'May I try this on?', zh:'我可以試穿嗎？',
    tip:'日本服裝店試衣間（フィッティングルーム）需要店員引導，主動詢問即可，店員通常很熱心協助。'
  },
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'もう少し大きいサイズはありますか？', roma:'Mō sukoshi ōkii saizu wa arimasu ka?',
    en:'Do you have a slightly bigger size?', zh:'有稍大一點的尺寸嗎？',
    tip:'日本尺寸偏小，亞洲旅客可能需要比平時大一至兩個號碼。也可說「もう少し小さい (小一點)」。'
  },
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'クレジットカードは使えますか？', roma:'Kurejitto-kādo wa tsukaemasu ka?',
    en:'Can I use a credit card?', zh:'可以刷信用卡嗎？',
    tip:'日本現金使用仍很普遍，尤其小店和神社。建議隨時攜帶足夠現金備用。'
  },
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'免税の手続きをしたいのですが', roma:'Menzei no tetsuzuki wo shitai no desu ga',
    en:'I\'d like to apply for tax exemption', zh:'我想辦理退稅手續',
    tip:'單次消費滿5,000日圓（未含稅）可辦理免稅，需出示護照。百貨公司通常在特定樓層設有免稅服務台。'
  },
  {
    cat:'shopping', catLabel:'購物 · 買い物',
    jp:'ギフト包装していただけますか？', roma:'Gifuto-hōsō shite itadakemasu ka?',
    en:'Could you gift wrap this please?', zh:'可以幫我做禮物包裝嗎？',
    tip:'日本的包裝文化（包み）非常精緻，百貨公司和高級店舖通常提供精美禮品包裝服務。'
  },

  // ── SIGHTSEEING ──
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'写真を撮ってもいいですか？', roma:'Shashin wo totte mo ii desu ka?',
    en:'May I take a photo here?', zh:'我可以在這裡拍照嗎？',
    tip:'神社寺廟的某些區域禁止攝影，務必尊重「撮影禁止 (no photography)」標示。在拍攝他人前也要先詢問。'
  },
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'入場料はいくらですか？', roma:'Nyūjō-ryō wa ikura desu ka?',
    en:'What is the admission fee?', zh:'入場費是多少？',
    tip:'日本許多神社免費，但博物館、公園等需購票。學生通常有優惠（学生割引 gakusei-waribiki）。'
  },
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'おすすめの観光地はどこですか？', roma:'Osusume no kankō-chi wa doko desu ka?',
    en:'What sightseeing spots do you recommend?', zh:'您推薦哪些觀光景點？',
    tip:'詢問當地人的建議往往比網路攻略更道地！日本人通常樂於幫助外國旅客，不必害羞開口。'
  },
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'ここで写真を撮っていただけますか？', roma:'Koko de shashin wo totte itadakemasu ka?',
    en:'Could you take a photo of me here?', zh:'可以在這裡幫我拍照嗎？',
    tip:'請人幫拍照的禮貌表達。事先想好構圖指給對方看，並在拍完後說「ありがとうございます！」。'
  },
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'何時に閉まりますか？', roma:'Nanji ni shimarimasu ka?',
    en:'What time do you close?', zh:'幾點關閉？',
    tip:'開放時間詢問「何時に開きますか？(幾點開放？)」，末班入場時間通常比閉館早30分鐘至1小時。'
  },
  {
    cat:'sightseeing', catLabel:'觀光 · 観光',
    jp:'日本語のガイドマップはありますか？', roma:'Nihongo no gaido-mappu wa arimasu ka?',
    en:'Is there a guide map available?', zh:'有導覽地圖嗎？',
    tip:'台灣旅客也可詢問是否有中文版：「中国語のマップはありますか？」。遊客中心（観光案内所）通常免費提供。'
  },

  // ── EMERGENCY ──
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'助けてください！', roma:'Tasukete kudasai!',
    en:'Help me, please!', zh:'請幫幫我！',
    tip:'緊急求救！日本急救電話：119（救護車／消防）、110（警察）。大聲清楚說出，日本人一定會立刻來協助。'
  },
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'救急車を呼んでください', roma:'Kyūkyūsha wo yonde kudasai',
    en:'Please call an ambulance', zh:'請叫救護車',
    tip:'日本急救電話是119，免費且服務效率極高。如在旅館，直接請前台代為聯絡最為迅速。'
  },
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'財布を盗まれました', roma:'Saifu wo nusumaremashita',
    en:'My wallet was stolen', zh:'我的錢包被偷了',
    tip:'立刻前往最近的派出所（交番 kōban）報案，可申請「被害届 (Higai-todoke)」報案證明，保險理賠需要此文件。'
  },
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'病院はどこですか？', roma:'Byōin wa doko desu ka?',
    en:'Where is the hospital?', zh:'醫院在哪裡？',
    tip:'旅行前建議查好最近提供外語服務的醫院。日本各地都有「Japan Helpline」(0120-461-997) 提供24小時英語協助。'
  },
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'日本語があまりわかりません', roma:'Nihongo ga amari wakarimasen',
    en:'I don\'t understand Japanese well', zh:'我日語不太好',
    tip:'說完後可加「英語か中国語は話せますか？(Could you speak English or Chinese?)」，在大城市通常能找到會英語的人。'
  },
  {
    cat:'emergency', catLabel:'緊急 · 緊急',
    jp:'体の具合が悪いです', roma:'Karada no guai ga warui desu',
    en:'I\'m not feeling well', zh:'我身體不舒服',
    tip:'具體說明症狀更有效：「頭が痛い (頭痛)」「熱があります (發燒)」「吐き気がします (想吐)」。'
  },
];

// ═══════════════════════════════════════════════
//   STATE
// ═══════════════════════════════════════════════
let activeCat = 'all';
let activeModal = null;
let speechQueue = null;

// ═══════════════════════════════════════════════
//   RENDER CARDS
// ═══════════════════════════════════════════════
function getFiltered() {
  const q = document.getElementById('searchInput').value.trim().toLowerCase();
  let data = activeCat === 'all' ? PHRASES : PHRASES.filter(p => p.cat === activeCat);
  if (q) {
    data = data.filter(p =>
      p.jp.includes(q) ||
      p.roma.toLowerCase().includes(q) ||
      p.en.toLowerCase().includes(q) ||
      p.zh.includes(q) ||
      p.catLabel.toLowerCase().includes(q)
    );
  }
  return data;
}

function renderCards() {
  const data = getFiltered();
  const grid = document.getElementById('cardsGrid');
  const countEl = document.getElementById('showingCount');

  countEl.textContent = data.length + ' phrases';
  document.getElementById('totalCount').textContent = PHRASES.length;

  if (data.length === 0) {
    grid.innerHTML = `
      <div class="empty-state">
        <div class="empty-state-jp">見つかりませんでした</div>
        <div class="empty-state-en">No phrases found — try a different keyword</div>
      </div>`;
    return;
  }

  grid.innerHTML = data.map((p, i) => {
    const idx = PHRASES.indexOf(p);
    return `
    <div class="card" style="animation-delay:${Math.min(i,20)*0.04}s" onclick="openModal(${idx})">
      <div class="card-cat">${p.catLabel}</div>
      <div class="card-jp">${p.jp}</div>
      <div class="card-roma">${p.roma}</div>
      <div class="card-en">${p.en}</div>
      <div class="card-zh">${p.zh}</div>
      <div class="card-footer">
        <button class="speak-btn speak-jp" onclick="event.stopPropagation(); speakCard(this, ${JSON.stringify(p.jp)}, 'ja-JP')">
          <div class="wave-icon"><span></span><span></span><span></span><span></span><span></span></div>
          日本語
        </button>
        <button class="speak-btn speak-en" onclick="event.stopPropagation(); speakCard(this, ${JSON.stringify(p.en.split('/')[0].trim())}, 'en-US')">
          <div class="wave-icon"><span></span><span></span><span></span><span></span><span></span></div>
          English
        </button>
      </div>
    </div>`;
  }).join('');
}

// ═══════════════════════════════════════════════
//   CATEGORY FILTER
// ═══════════════════════════════════════════════
function setCategory(cat, btn) {
  activeCat = cat;
  document.querySelectorAll('.tab').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderCards();
}

// ═══════════════════════════════════════════════
//   SPEECH API
// ═══════════════════════════════════════════════
function speak(text, lang, onEnd) {
  if (!window.speechSynthesis) {
    alert('您的瀏覽器不支援語音功能。\nYour browser does not support speech synthesis.');
    return;
  }
  window.speechSynthesis.cancel();
  const utter = new SpeechSynthesisUtterance(text);
  utter.lang  = lang;
  utter.rate  = lang === 'ja-JP' ? 0.85 : 0.9;
  utter.pitch = 1.05;
  if (onEnd) utter.onend = utter.onerror = onEnd;

  // Fix Chrome bug: voices may not load immediately
  const trySpeak = () => {
    const voices = window.speechSynthesis.getVoices();
    if (voices.length) {
      const preferred = voices.find(v => v.lang === lang && v.localService);
      if (preferred) utter.voice = preferred;
    }
    window.speechSynthesis.speak(utter);
  };

  if (window.speechSynthesis.getVoices().length) {
    trySpeak();
  } else {
    window.speechSynthesis.addEventListener('voiceschanged', trySpeak, { once: true });
    window.speechSynthesis.speak(utter); // fallback
  }
}

// Card inline speak buttons
let currentSpeakBtn = null;

function speakCard(btn, text, lang) {
  // Toggle off if same button
  if (currentSpeakBtn === btn) {
    window.speechSynthesis && window.speechSynthesis.cancel();
    btn.classList.remove('playing');
    currentSpeakBtn = null;
    return;
  }
  if (currentSpeakBtn) currentSpeakBtn.classList.remove('playing');
  btn.classList.add('playing');
  currentSpeakBtn = btn;
  speak(text, lang, () => {
    btn.classList.remove('playing');
    if (currentSpeakBtn === btn) currentSpeakBtn = null;
  });
}

// ═══════════════════════════════════════════════
//   MODAL
// ═══════════════════════════════════════════════
function openModal(idx) {
  const p = PHRASES[idx];
  activeModal = p;

  document.getElementById('mCat').textContent  = p.catLabel;
  document.getElementById('mJp').textContent   = p.jp;
  document.getElementById('mRoma').textContent = p.roma;
  document.getElementById('mEn').textContent   = p.en;
  document.getElementById('mZh').textContent   = p.zh;
  document.getElementById('mTip').textContent  = p.tip;

  // Reset speak buttons
  document.getElementById('mBtnJp').classList.remove('playing');
  document.getElementById('mBtnEn').classList.remove('playing');
  window.speechSynthesis && window.speechSynthesis.cancel();

  document.getElementById('overlay').classList.add('open');
  document.body.style.overflow = 'hidden';
}

function closeModal() {
  document.getElementById('overlay').classList.remove('open');
  document.body.style.overflow = '';
  window.speechSynthesis && window.speechSynthesis.cancel();
  document.getElementById('mBtnJp').classList.remove('playing');
  document.getElementById('mBtnEn').classList.remove('playing');
  activeModal = null;
}

function handleOverlayClick(e) {
  if (e.target === document.getElementById('overlay')) closeModal();
}

function speakModal(which) {
  if (!activeModal) return;
  const jpBtn = document.getElementById('mBtnJp');
  const enBtn = document.getElementById('mBtnEn');

  if (which === 'jp') {
    // Toggle
    if (jpBtn.classList.contains('playing')) {
      window.speechSynthesis.cancel();
      jpBtn.classList.remove('playing');
      return;
    }
    enBtn.classList.remove('playing');
    jpBtn.classList.add('playing');
    speak(activeModal.jp, 'ja-JP', () => jpBtn.classList.remove('playing'));
  } else {
    if (enBtn.classList.contains('playing')) {
      window.speechSynthesis.cancel();
      enBtn.classList.remove('playing');
      return;
    }
    jpBtn.classList.remove('playing');
    enBtn.classList.add('playing');
    const enText = activeModal.en.split('/')[0].trim();
    speak(enText, 'en-US', () => enBtn.classList.remove('playing'));
  }
}

// Keyboard close
document.addEventListener('keydown', e => {
  if (e.key === 'Escape') closeModal();
});

// ═══════════════════════════════════════════════
//   SEARCH
// ═══════════════════════════════════════════════
document.getElementById('searchInput').addEventListener('input', () => {
  // Reset to all when searching
  if (activeCat !== 'all') {
    activeCat = 'all';
    document.querySelectorAll('.tab').forEach(b => b.classList.remove('active'));
    document.querySelector('.tab[data-cat="all"]').classList.add('active');
  }
  renderCards();
});

// ═══════════════════════════════════════════════
//   INIT
// ═══════════════════════════════════════════════
renderCards();
</script>
</body>
</html>
